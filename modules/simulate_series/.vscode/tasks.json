{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/opt/intel/oneapi/compiler/2021.2.0/linux/compiler/lib/intel64_lin/;source /opt/intel/oneapi/setvars.sh --force;  make clean;make ",
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ]
}